#include <iostream>
#include<stdio.h>

using namespace std;

int main()
{
    int n;
    cout<<"Enter size";
    cin>>n;
    int A[n]; //variable size arrays cannot be initialized while declaration
    A[0]=1;
    
    for (int x:A)
    {
        cout<<x<<endl;
    }
    
    return 0;
}
